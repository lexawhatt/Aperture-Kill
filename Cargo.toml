[package]
name = "portals"
version = "0.1.0"
edition = "2024"

[dependencies]
glam = "0.27"
rodio = { version = "0.19", default-features = false, features = ["wav"] }
softbuffer = "0.4"
winit = "0.30"
